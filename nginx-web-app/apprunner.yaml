version: 1.0
runtime: nodejs12
build:
  commands:
    - npm install
run:
  command: node ./bin/www
  network:
    port: 80
observability:
  metrics:
    - name: ActiveConnections
      unit: Count
      timeAggregation: Average
  tracing:
    vendor: awsxray