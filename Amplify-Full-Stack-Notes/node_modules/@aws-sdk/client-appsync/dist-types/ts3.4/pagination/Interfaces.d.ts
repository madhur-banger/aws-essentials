import { PaginationConfiguration } from "@smithy/types";
import { AppSyncClient } from "../AppSyncClient";
export interface AppSyncPaginationConfiguration
  extends PaginationConfiguration {
  client: AppSyncClient;
}
