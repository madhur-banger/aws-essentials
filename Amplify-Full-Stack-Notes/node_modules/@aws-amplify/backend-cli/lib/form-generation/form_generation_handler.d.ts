import { DeployedBackendIdentifier } from '@aws-amplify/deployed-backend-client';
import { AWSClientProvider } from '@aws-amplify/plugin-types';
import { AmplifyClient } from '@aws-sdk/client-amplify';
import { CloudFormationClient } from '@aws-sdk/client-cloudformation';
type FormGenerationParams = {
    modelsOutDir: string;
    uiOutDir: string;
    apiUrl: string;
    backendIdentifier: DeployedBackendIdentifier;
    modelsFilter?: string[];
};
type FormGenerationInstanceOptions = {
    awsClientProvider: AWSClientProvider<{
        getAmplifyClient: AmplifyClient;
        getCloudFormationClient: CloudFormationClient;
    }>;
};
/**
 * Creates a handler for FormGeneration
 */
export declare class FormGenerationHandler {
    private readonly formGenParams;
    /**
     * Instantiates the handler
     */
    constructor(formGenParams: FormGenerationInstanceOptions);
    generate: (params: FormGenerationParams) => Promise<void>;
    private logMessages;
}
export {};
//# sourceMappingURL=form_generation_handler.d.ts.map