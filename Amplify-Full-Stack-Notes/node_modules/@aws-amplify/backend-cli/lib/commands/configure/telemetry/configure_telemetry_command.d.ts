import { ConfigurationController } from '@aws-amplify/platform-core';
import { Argv, CommandModule } from 'yargs';
/**
 * Command to configure AWS Amplify profile.
 */
export declare class ConfigureTelemetryCommand implements CommandModule<object> {
    private readonly configController;
    /**
     * @inheritDoc
     */
    readonly command: string;
    /**
     * @inheritDoc
     */
    readonly describe: string;
    /**
     * Configure profile command.
     */
    constructor(configController: ConfigurationController);
    /**
     * @inheritDoc
     */
    handler: () => never;
    /**
     * @inheritDoc
     */
    builder: (yargs: Argv) => Argv<{}>;
}
//# sourceMappingURL=configure_telemetry_command.d.ts.map