import { Options } from 'open';
import { ChildProcess } from 'child_process';
/**
 * Helper class to open apps (URLs, files, executable). Cross-platform.
 */
export declare class Open {
    static open: (target: string, options: Options) => Promise<ChildProcess | void>;
    static handleOpenError: (err: Error, target: string) => void;
}
//# sourceMappingURL=open.d.ts.map