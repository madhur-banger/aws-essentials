import { DeployedBackendIdentifier } from '@aws-amplify/deployed-backend-client';
import { GenerateOptions, GenerationResult } from '@aws-amplify/model-generator';
import { AWSClientProvider } from '@aws-amplify/plugin-types';
import { AmplifyClient } from '@aws-sdk/client-amplify';
import { CloudFormationClient } from '@aws-sdk/client-cloudformation';
import { S3Client } from '@aws-sdk/client-s3';
export type InvokeGenerateApiCodeProps = GenerateOptions & DeployedBackendIdentifier;
/**
 * Class to wrap static generateApiCode method to facilitate testing.
 */
export declare class GenerateApiCodeAdapter {
    private readonly awsClientProvider;
    /**
     * Creates graphql api code adapter.
     */
    constructor(awsClientProvider: AWSClientProvider<{
        getAmplifyClient: AmplifyClient;
        getCloudFormationClient: CloudFormationClient;
        getS3Client: S3Client;
    }>);
    /**
     * Invoke the generateApiCode method, using the constructor injected credentialProvider, and remaining props.
     */
    invokeGenerateApiCode: (props: InvokeGenerateApiCodeProps) => Promise<GenerationResult>;
}
//# sourceMappingURL=generate_api_code_adapter.d.ts.map