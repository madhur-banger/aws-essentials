import { Argv } from 'yargs';
import { UsageDataEmitter } from '@aws-amplify/platform-core';
/**
 * An error that has both output and error that occurred during command execution.
 */
export declare class TestCommandError extends Error {
    readonly error: Error;
    readonly output: string;
    /**
     * Creates new test command error.
     */
    constructor(error: Error, output: string);
}
/**
 * Runs commands given preconfigured yargs parser.
 */
export declare class TestCommandRunner {
    private parser;
    private usageDataEmitter;
    /**
     * Creates new command runner.
     */
    constructor(parser: Argv, usageDataEmitter?: UsageDataEmitter);
    /**
     * Runs a command. Returns command output or throws an error if command failed.
     */
    runCommand: (args: string | Array<string>) => Promise<string>;
}
//# sourceMappingURL=command_runner.d.ts.map