import { ClientConfigFormat, ClientConfigVersion } from '@aws-amplify/client-config';
import { ClientConfigGeneratorAdapter } from './client_config_generator_adapter.js';
import { DeployedBackendIdentifier } from '@aws-amplify/deployed-backend-client';
/**
 * Life cycle handler for client config files written to file system
 */
export declare class ClientConfigLifecycleHandler {
    private clientConfigGeneratorAdapter;
    private readonly version;
    private readonly outDir?;
    private readonly format?;
    /**
     * Instantiate a new life cycle handler which proxy requests to ClientConfigGeneratorAdapter
     */
    constructor(clientConfigGeneratorAdapter: ClientConfigGeneratorAdapter, version: ClientConfigVersion, outDir?: string | undefined, format?: ClientConfigFormat | undefined);
    generateClientConfigFile: (backendIdentifier: DeployedBackendIdentifier) => Promise<void>;
    deleteClientConfigFile: () => Promise<void>;
}
//# sourceMappingURL=client_config_lifecycle_handler.d.ts.map