import { ClientConfig, ClientConfigFormat, ClientConfigVersion } from '@aws-amplify/client-config';
import { DeployedBackendIdentifier } from '@aws-amplify/deployed-backend-client';
import { AWSClientProvider } from '@aws-amplify/plugin-types';
import { S3Client } from '@aws-sdk/client-s3';
import { AmplifyClient } from '@aws-sdk/client-amplify';
import { CloudFormationClient } from '@aws-sdk/client-cloudformation';
/**
 * Adapts static generateClientConfigToFile from @aws-amplify/client-config call to make it injectable and testable.
 */
export declare class ClientConfigGeneratorAdapter {
    private readonly awsClientProvider;
    /**
     * Creates new adapter for generateClientConfigToFile from @aws-amplify/client-config.
     */
    constructor(awsClientProvider: AWSClientProvider<{
        getS3Client: S3Client;
        getAmplifyClient: AmplifyClient;
        getCloudFormationClient: CloudFormationClient;
    }>);
    /**
     * Generates the client configuration for a given backend
     */
    generateClientConfig: (backendIdentifier: DeployedBackendIdentifier, version: ClientConfigVersion) => Promise<ClientConfig>;
    /**
     * Calls generateClientConfigToFile from @aws-amplify/client-config.
     * @see generateClientConfigToFile for more information.
     */
    generateClientConfigToFile: (backendIdentifier: DeployedBackendIdentifier, version: ClientConfigVersion, outDir?: string, format?: ClientConfigFormat) => Promise<void>;
}
//# sourceMappingURL=client_config_generator_adapter.d.ts.map