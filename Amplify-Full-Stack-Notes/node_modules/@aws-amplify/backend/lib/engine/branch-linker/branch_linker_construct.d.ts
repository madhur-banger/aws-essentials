import { Construct } from 'constructs';
import { BackendIdentifier } from '@aws-amplify/plugin-types';
/**
 * Adds a custom resources that links and un-links branch deployments
 * to Amplify Console.
 */
export declare class AmplifyBranchLinkerConstruct extends Construct {
    /**
     * Creates Amplify Console linker construct.
     */
    constructor(scope: Construct, backendIdentifier: BackendIdentifier);
}
//# sourceMappingURL=branch_linker_construct.d.ts.map