/**
 * Generates a typed process.env shim for environment variables
 */
export declare class FunctionEnvironmentTypeGenerator {
    private readonly functionName;
    private readonly header;
    private readonly envAssignment;
    private typeDefFilePath;
    private indentation;
    /**
     * Initialize typed process.env shim file name and location
     */
    constructor(functionName: string);
    /**
     * Generate a typed process.env shim
     */
    generateTypedProcessEnvShim(amplifyBackendEnvVars: string[]): void;
    /**
     * Generate an any-typed process.env shim if doesn't exist
     */
    generateProcessEnvShim: () => void;
    private writeShimFile;
}
//# sourceMappingURL=function_env_type_generator.d.ts.map