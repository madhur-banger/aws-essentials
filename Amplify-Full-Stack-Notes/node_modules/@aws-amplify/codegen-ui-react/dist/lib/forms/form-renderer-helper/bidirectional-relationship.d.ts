import { GenericDataSchema } from '@aws-amplify/codegen-ui';
import { FieldConfigMetadata } from '@aws-amplify/codegen-ui/lib/types';
import { ImportCollection } from '../../imports';
import { DataApiKind } from '../../react-render-config';
export declare function getBiDirectionalRelationshipStatements({ formActionType, dataSchema, importCollection, fieldConfig, modelName, savedRecordName, thisModelPrimaryKeys, dataApi, renderConfigDependencies, }: {
    formActionType: 'create' | 'update';
    dataSchema: GenericDataSchema;
    importCollection: ImportCollection;
    fieldConfig: [string, FieldConfigMetadata];
    modelName: string;
    savedRecordName: string;
    thisModelPrimaryKeys: string[];
    dataApi?: DataApiKind;
    renderConfigDependencies?: {
        [key: string]: string;
    };
}): any[];
