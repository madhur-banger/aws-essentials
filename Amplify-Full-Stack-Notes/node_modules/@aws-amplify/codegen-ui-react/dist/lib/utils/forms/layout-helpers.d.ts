import { ComponentMetadata } from '@aws-amplify/codegen-ui';
/**
 * Detemrines if a form has any layout configs to conditionally render theme hooks
 */
export declare function hasTokenReference(componentMetadata: ComponentMetadata): boolean;
