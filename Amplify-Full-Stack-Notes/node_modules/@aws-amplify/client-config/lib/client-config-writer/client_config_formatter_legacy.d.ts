import { ClientConfig, ClientConfigFormat } from '../client-config-types/client_config.js';
import { ClientConfigMobileConverter } from './client_config_to_mobile_legacy_converter.js';
import { ClientConfigFormatter } from './client_config_formatter.js';
/**
 * Formats client config to desired format.
 */
export declare class ClientConfigFormatterLegacy implements ClientConfigFormatter {
    private readonly configConverter;
    /**
     * Creates new client config formatter.
     */
    constructor(configConverter: ClientConfigMobileConverter);
    format: (clientConfig: ClientConfig, format: ClientConfigFormat) => string;
}
//# sourceMappingURL=client_config_formatter_legacy.d.ts.map