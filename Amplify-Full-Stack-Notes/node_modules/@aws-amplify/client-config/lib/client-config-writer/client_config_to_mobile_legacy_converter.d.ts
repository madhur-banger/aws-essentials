import { ClientConfigLegacy } from '../client-config-types/client_config.js';
import { ClientConfigMobile } from '../client-config-types/mobile/client_config_mobile_types.js';
/**
 * Converts client config to a different shapes needed by mobile libraries for Gen1 format.
 */
export declare class ClientConfigMobileConverter {
    private readonly packageName;
    private readonly packageVersion;
    /**
     * Creates client config converter
     */
    constructor(packageName: string, packageVersion: string);
    /**
     * Converts client config to a shape consumable by mobile libraries.
     */
    convertToMobileConfig: (clientConfig: ClientConfigLegacy) => ClientConfigMobile;
}
//# sourceMappingURL=client_config_to_mobile_legacy_converter.d.ts.map