import { BackendIdentifier, MainStackNameResolver } from '@aws-amplify/plugin-types';
/**
 * Resolves the main stack name for a given project environment
 */
export declare class BackendIdentifierMainStackNameResolver implements MainStackNameResolver {
    private readonly backendId;
    /**
     * Initialize with the project environment identifier and an SSMClient
     */
    constructor(backendId: BackendIdentifier);
    /**
     * Resolve the stack name for this project environment
     */
    resolveMainStackName: () => Promise<string>;
}
//# sourceMappingURL=backend_identifier_main_stack_name_resolver.d.ts.map