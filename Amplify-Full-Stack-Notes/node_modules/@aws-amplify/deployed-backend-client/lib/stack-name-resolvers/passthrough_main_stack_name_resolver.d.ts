import { MainStackNameResolver } from '@aws-amplify/plugin-types';
export type StackIdentifier = {
    stackName: string;
};
/**
 * A pass-through implementation of MainStackNameResolver for when the stack name of a backend is already known
 */
export declare class PassThroughMainStackNameResolver implements MainStackNameResolver {
    private readonly stackNameIdentifier;
    /**
     * Initialize with the known stack name
     */
    constructor(stackNameIdentifier: StackIdentifier);
    /**
     * Return the stack name
     */
    resolveMainStackName: () => Promise<string>;
}
//# sourceMappingURL=passthrough_main_stack_name_resolver.d.ts.map