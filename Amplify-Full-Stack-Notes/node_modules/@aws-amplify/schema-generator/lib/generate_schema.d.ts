export type SchemaGeneratorConfig = {
    connectionUri: {
        secretName: string;
        value: string;
    };
    sslCert?: {
        secretName: string;
        value: string;
    };
    out: string;
};
/**
 * Schema generator class.
 */
export declare class SchemaGenerator {
    generate: (props: SchemaGeneratorConfig) => Promise<void>;
}
export type SQLEngine = 'mysql' | 'postgresql';
export type SQLDataSourceConfig = {
    engine: SQLEngine;
    username: string;
    password: string;
    host: string;
    database: string;
    port: number;
};
/**
 * Parses database URL into a configuration object.
 */
export declare const parseDatabaseUrl: (databaseUrl: string) => SQLDataSourceConfig;
//# sourceMappingURL=generate_schema.d.ts.map