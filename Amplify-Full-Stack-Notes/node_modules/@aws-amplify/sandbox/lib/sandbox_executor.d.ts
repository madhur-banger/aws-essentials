import { BackendIdentifier } from '@aws-amplify/plugin-types';
import { BackendDeployer, DeployResult, DestroyResult } from '@aws-amplify/backend-deployer';
import { SecretClient } from '@aws-amplify/backend-secret';
import { Printer } from '@aws-amplify/cli-core';
/**
 * Execute CDK commands.
 */
export declare class AmplifySandboxExecutor {
    private readonly backendDeployer;
    private readonly secretClient;
    private readonly printer;
    /**
     * Function that invokes the callback with debounce.
     * Debounce is needed in case multiple duplicate events are received.
     */
    private invoke;
    /**
     * Creates an AmplifySandboxExecutor instance
     */
    constructor(backendDeployer: BackendDeployer, secretClient: SecretClient, printer: Printer);
    /**
     * Deploys sandbox
     */
    deploy: (backendId: BackendIdentifier, validateAppSourcesProvider: () => boolean, profile: string | undefined) => Promise<DeployResult>;
    /**
     * Destroy sandbox. Do not swallow errors
     */
    destroy: (backendId: BackendIdentifier, profile: string | undefined) => Promise<DestroyResult>;
    private getSecretLastUpdated;
}
//# sourceMappingURL=sandbox_executor.d.ts.map