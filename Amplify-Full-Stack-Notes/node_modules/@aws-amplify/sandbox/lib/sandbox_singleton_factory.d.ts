import { Format, Printer } from '@aws-amplify/cli-core';
import { BackendIdSandboxResolver, Sandbox } from './sandbox.js';
/**
 * Factory to create a new sandbox
 */
export declare class SandboxSingletonFactory {
    private readonly sandboxIdResolver;
    private readonly printer;
    private readonly format;
    private instance;
    /**
     * sandboxIdResolver allows sandbox to lazily load the sandbox backend id on demand
     */
    constructor(sandboxIdResolver: BackendIdSandboxResolver, printer: Printer, format: Format);
    /**
     * Returns a singleton instance of a Sandbox
     */
    getInstance: () => Promise<Sandbox>;
}
//# sourceMappingURL=sandbox_singleton_factory.d.ts.map