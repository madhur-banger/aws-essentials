import { FormDefinition, StudioFieldPosition } from '../../types';
/**
 * Helper that returns the row and column indices of a string in a two-dimensional array
 */
export declare const findIndices: (name: string, matrix: string[][]) => [number, number] | undefined;
export declare function removeFromMatrix(indices: [number, number], formDefinition: FormDefinition): void;
export declare function removeAndReturnItemOnward(indices: [number, number], formDefinition: FormDefinition): string[];
/**
 * Impure function that maps the element matrix of form definition
 * from a queue of sectional and input elements.
 */
export declare function mapElementMatrix({ elementQueue, formDefinition, }: {
    formDefinition: FormDefinition;
    elementQueue: {
        name: string;
        position?: StudioFieldPosition;
        excluded?: boolean;
    }[];
}): void;
//# sourceMappingURL=position.d.ts.map