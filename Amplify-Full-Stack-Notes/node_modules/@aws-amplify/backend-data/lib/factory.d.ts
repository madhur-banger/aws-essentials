import { ConstructFactory, ConstructFactoryGetInstanceProps } from '@aws-amplify/plugin-types';
import { AmplifyData } from '@aws-amplify/data-construct';
import { DataProps } from './types.js';
/**
 * Singleton factory for AmplifyGraphqlApi constructs that can be used in Amplify project files.
 *
 * Exported for testing purpose only & should NOT be exported out of the package.
 */
export declare class DataFactory implements ConstructFactory<AmplifyData> {
    private readonly props;
    private readonly importStack;
    static factoryCount: number;
    private generator;
    /**
     * Create a new AmplifyConstruct
     */
    constructor(props: DataProps, importStack?: string | undefined);
    /**
     * Gets an instance of the Data construct
     */
    getInstance: (props: ConstructFactoryGetInstanceProps) => AmplifyData;
}
/**
 * Creates a factory that implements ConstructFactory<AmplifyGraphqlApi>
 */
export declare const defineData: (props: DataProps) => ConstructFactory<AmplifyData>;
//# sourceMappingURL=factory.d.ts.map