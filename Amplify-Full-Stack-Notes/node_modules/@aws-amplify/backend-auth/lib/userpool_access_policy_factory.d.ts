import { IUserPool } from 'aws-cdk-lib/aws-cognito';
import { Policy } from 'aws-cdk-lib/aws-iam';
import { AuthAction } from './types.js';
/**
 * Generates IAM policies scoped to a single userpool.
 */
export declare class UserPoolAccessPolicyFactory {
    private readonly userpool;
    private readonly namePrefix;
    private readonly stack;
    private policyCount;
    /**
     * Instantiate with the userpool to generate policies for
     */
    constructor(userpool: IUserPool);
    createPolicy: (actions: AuthAction[]) => Policy;
}
//# sourceMappingURL=userpool_access_policy_factory.d.ts.map