import { AppSyncClient } from '@aws-sdk/client-appsync';
/**
 * Fetches the introspection schema for a given api id
 */
export declare class AppSyncIntrospectionSchemaFetcher {
    private appSyncClient;
    /**
     * Instantiates the schema fetcher with an appsync client
     */
    constructor(appSyncClient: AppSyncClient);
    fetch: (apiId: string) => Promise<string>;
}
//# sourceMappingURL=appsync_schema_fetcher.d.ts.map